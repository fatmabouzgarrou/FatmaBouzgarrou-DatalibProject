# Installation de DataLib

Pour installer **DataLib**, vous pouvez utiliser `pip`, le gestionnaire de paquets Python. Suivez les étapes ci-dessous :

## Étape 1 : Installer via pip

```bash
pip install datalib
